FROM python:2.7
ADD requirements/ /app/requirements
ADD btc_transfer/ /app/btc_transfer
ADD setup.py      /app/
ADD bin/ /app/bin
WORKDIR /app/

RUN pip install -r requirements/server.txt
RUN pip install -e .
ENTRYPOINT ./bin/tokens_transferer -vv

