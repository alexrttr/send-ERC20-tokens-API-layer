#!/usr/bin/env python

from setuptools import setup

setup(name='BTC to ETH transfer',
      version='0.1dev',
      description=('BTC to ETH transfer tool'),
      packages=['btc_transfer'],
      )
